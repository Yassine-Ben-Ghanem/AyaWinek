{% extends 'base.html.twig' %}
   {% block nav %}
{% if  is_granted('ROLE_ADMIN')%}
  
              <li><a href="{{path('ride_new')}}">Add Ride</a></li>
              <li><a href="{{path('user')}}">users</a></li>
   
            {% endif %}

            <li><a href="{{path('app_logout')}}">LogOut</a></li>
 {% endblock %} 
{% block body %}
    <table style=" border-collapse: collapse;">
  
        <tbody font: 110%/1.4 system-ui;
  margin: 0;
  padding: 10rem 2rem 50rem;>
        {% for user in users %}
            {# <tr>
                <td>{{ driver.id }}</td>
                <td>{{ driver.firstMame }}</td>
                <td>{{ driver.lastName }}</td>
                <td>{{ driver.numberTel }}</td>
                <td>{{ driver.accountEmail }}</td>
                <td>
                    <a href="{{ path('driver_show', {'id': driver.id}) }}">show</a>
                    <a href="{{ path('driver_edit', {'id': driver.id}) }}">edit</a>
                </td>
            </tr> #}
             <tr>
      <td style=" padding: 1rem;">{{ user.id }}</td>
      <td style=" padding: 1rem;">{{ user.getFirstName }}</td>
      <td style=" padding: 1rem;"> {{ user.getLastName }}</td>
      {# <td style=" padding: 1rem;">C{{ user.getRoles() }}</td> #}
      <td style=" padding: 1rem;">{{ user.email }}</td>
    </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
