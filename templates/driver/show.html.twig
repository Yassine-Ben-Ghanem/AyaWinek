{% extends 'base.html.twig' %}

{% block title %}Driver{% endblock %}

{% block body %}
    <h1>Driver</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ driver.id }}</td>
            </tr>
            <tr>
                <th>First_mame</th>
                <td>{{ driver.firstMame }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ driver.lastName }}</td>
            </tr>
            <tr>
                <th>Number_tel</th>
                <td>{{ driver.numberTel }}</td>
            </tr>
            <tr>
                <th>Account_email</th>
                <td>{{ driver.accountEmail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('driver_index') }}">back to list</a>

    <a href="{{ path('driver_edit', {'id': driver.id}) }}">edit</a>

    {{ include('driver/_delete_form.html.twig') }}
{% endblock %}
